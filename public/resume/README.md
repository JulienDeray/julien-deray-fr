# Résumé

Drop the rendercv-generated PDF here as `julien-deray-resume.pdf`.

The site links to it from the "Download résumé" button on the Journey section
(see `src/data/site.ts` → `resumeUrl`).

Generated from `~/Documents/Works/CV/Julien_Deray_CV.yaml` with:

```
rendercv render Julien_Deray_CV.yaml
```

Keep the filename stable (`julien-deray-resume.pdf`) so the link never breaks.
The journey timeline mirrors this CV — update both when a role changes.
